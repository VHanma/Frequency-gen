name: Build LightCode Photophone Loop APK

on:
  workflow_dispatch:
  push:
    branches:
      - 'agent/lightcode-photophone-loop'
    paths:
      - 'lightcode-jar/photophone/**'
      - '.github/workflows/build-lightcode-photophone-loop-apk.yml'
  pull_request:
    branches:
      - 'agent/lightcode-photophone'
      - main
    paths:
      - 'lightcode-jar/photophone/**'
      - '.github/workflows/build-lightcode-photophone-loop-apk.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lightcode-jar

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build Loop debug APK
        id: loop_build
        continue-on-error: true
        shell: bash
        run: |
          set +e
          gradle --no-daemon --stacktrace :photophone:assembleDebug 2>&1 | tee loop-build.log
          code=${PIPESTATUS[0]}
          echo "$code" > loop-build-exit.txt
          exit 0

      - name: Upload compiler log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LightCode-Photophone-Loop-build-log
          path: |
            lightcode-jar/loop-build.log
            lightcode-jar/loop-build-exit.txt
          if-no-files-found: error

      - name: Fail when compiler failed
        shell: bash
        run: |
          code=$(cat loop-build-exit.txt)
          if [ "$code" != "0" ]; then
            echo "Loop compilation failed with exit code $code"
            exit "$code"
          fi

      - name: Upload Loop APK
        uses: actions/upload-artifact@v4
        with:
          name: LightCode-Photophone-Loop-debug-apk
          path: lightcode-jar/photophone/build/outputs/apk/debug/photophone-debug.apk
          if-no-files-found: error
