name: DirectTube Omega v1.3 Bulk Build

on:
  push:
    paths:
      - '.github/workflows/_temporary-directtube-v13-bulk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout v1.3 bulk branch
        uses: actions/checkout@v4
        with:
          ref: directtube-omega-v1.3-bulk

      - name: Restore v1.3 UI and transcript engine
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import base64, gzip, re
          root = Path('DirectTubeOmega')
          java = root / 'app/src/main/java/com/rivalomega/directtube'
          java.mkdir(parents=True, exist_ok=True)

          p = root / 'MainActivity.v13.java.gz.b64'
          s = re.sub(r'\s+', '', p.read_text())
          s += '=' * (-len(s) % 4)
          main = java / 'MainActivity.java'
          main.write_bytes(gzip.decompress(base64.b64decode(s)))

          parts = sorted((root / 'chunks').glob('YouTubeTranscriptClient.java.part*'))
          if not parts:
              raise SystemExit('No YouTubeTranscriptClient chunks found')
          client = java / 'YouTubeTranscriptClient.java'
          client.write_text(''.join(x.read_text() for x in parts), encoding='utf-8')
          print('RESTORED_MAIN', main.stat().st_size)
          print('RESTORED_CLIENT', client.stat().st_size, len(parts))
          PY

          cp DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/MainActivity.java /tmp/MainActivity-v13.java
          cd DirectTubeOmega
          python3 tools/patch_fullest.py
          cp /tmp/MainActivity-v13.java app/src/main/java/com/rivalomega/directtube/MainActivity.java
          grep -q 'SiteTranscriptCompat.fetch' app/src/main/java/com/rivalomega/directtube/YouTubeTranscriptClient.java

      - name: Verify bulk feature is present
        shell: bash
        run: |
          SRC='DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/MainActivity.java'
          grep -n -Ei 'bulk|copy all|export all|queue' "$SRC" | head -120
          grep -q '1.3.0' DirectTubeOmega/app/build.gradle
          grep -qi 'Bulk transcribe' "$SRC"
          grep -q 'queue.size()' "$SRC"
          test -s DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/YouTubeTranscriptClient.java

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Install Android 35 packages using runner SDK
        shell: bash
        run: |
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          test -x "$SDKMANAGER"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" 'platforms;android-35' 'build-tools;35.0.0' 'platform-tools'
          echo "ANDROID_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_HOME" >> "$GITHUB_ENV"

      - name: Build debug APK
        working-directory: DirectTubeOmega
        run: gradle --no-daemon assembleDebug

      - name: Verify APK and package outputs
        shell: bash
        run: |
          APK='DirectTubeOmega/app/build/outputs/apk/debug/app-debug.apk'
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose "$APK"
          mkdir -p dist
          cp "$APK" dist/DirectTubeOmega-v1.3-Bulk.apk
          sha256sum dist/DirectTubeOmega-v1.3-Bulk.apk > dist/DirectTubeOmega-v1.3-Bulk.apk.sha256
          cp DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/MainActivity.java dist/MainActivity-v1.3-Bulk.java
          cd DirectTubeOmega
          zip -qr ../dist/DirectTubeOmega-v1.3-Bulk-source.zip . -x 'app/build/*' '.gradle/*'

      - name: Upload v1.3 bulk artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DirectTubeOmega-v1.3-Bulk
          path: dist/*
          if-no-files-found: error
