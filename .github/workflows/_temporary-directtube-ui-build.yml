# activate redesigned UI build
name: Temporary DirectTube UI v1.1 APK Build

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/_temporary-directtube-ui-build.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout redesigned DirectTube branch
        uses: actions/checkout@v4
        with:
          ref: directtube-omega-ui-v1.1

      - name: Restore redesigned UI source and extractor
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p DirectTubeOmega/app/src/main/java/com/rivalomega/directtube
          base64 -d DirectTubeOmega/MainActivity.java.gz.b64 | gzip -d > DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/MainActivity.java
          cat DirectTubeOmega/chunks/YouTubeTranscriptClient.java.part* > DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/YouTubeTranscriptClient.java
          cat > /tmp/source.sha256 <<'EOF'
          d84e8057c759e90a1c3437640c72919343bee10264a395298f102892b78df942  DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/MainActivity.java
          069e180f065801779f2164dcd68473e4979bca08f81f7a3648212b62a6a76feb  DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/YouTubeTranscriptClient.java
          656cf884497c6108ad40e2d9df5699939ce4d2bebd986608267587f88fac1fc9  DirectTubeOmega/app/src/main/java/com/rivalomega/directtube/TranscriptStore.java
          EOF
          sha256sum -c /tmp/source.sha256

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Install Android SDK 35
        shell: bash
        run: |
          set -euo pipefail
          export ANDROID_HOME=/usr/local/lib/android/sdk
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          test -x "$SDKMANAGER"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" 'platforms;android-35' 'build-tools;35.0.0' 'platform-tools'
          echo "ANDROID_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"
          echo "$ANDROID_HOME/platform-tools" >> "$GITHUB_PATH"
          echo "$ANDROID_HOME/build-tools/35.0.0" >> "$GITHUB_PATH"

      - name: Build debug APK
        shell: bash
        run: gradle --no-daemon -p DirectTubeOmega :app:assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          APK=DirectTubeOmega/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          apksigner verify --verbose "$APK"
          unzip -t "$APK" >/dev/null
          cp "$APK" DirectTubeOmega-v1.1-redesign.apk
          sha256sum DirectTubeOmega-v1.1-redesign.apk > DirectTubeOmega-v1.1-redesign.apk.sha256

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DirectTubeOmega-v1.1-Redesign-APK
          path: |
            DirectTubeOmega-v1.1-redesign.apk
            DirectTubeOmega-v1.1-redesign.apk.sha256
          if-no-files-found: error
          retention-days: 7
